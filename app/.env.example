# ===========================================================================
# RAG Knowledge Assistant — Environment Variables
# Copy this file to .env and fill in your values:  cp .env.example .env
# ===========================================================================

# ---- OpenAI / LLM --------------------------------------------------------
# Required: your OpenAI API key (also used for embeddings)
OPENAI_API_KEY=sk-...

# Model for chat/generation
LLM_MODEL=gpt-4o-mini

# Optional: point to a different OpenAI-compatible API
# Examples:
#   OpenRouter:  https://openrouter.ai/api/v1   (set LLM_MODEL=openai/gpt-4o-mini)
#   Ollama:      http://localhost:11434/v1       (set LLM_MODEL=llama3)
#   LM Studio:   http://localhost:1234/v1
# LLM_BASE_URL=

LLM_MAX_TOKENS=1024
LLM_TEMPERATURE=0.7
LLM_TIMEOUT=60.0
LLM_MAX_RETRIES=3

# ---- Embeddings ----------------------------------------------------------
EMBEDDING_MODEL_NAME=text-embedding-3-small
EMBEDDING_MODEL_PROVIDER=openai
EMBEDDING_DIMENSIONS=1536
EMBEDDING_BATCH_SIZE=100
EMBEDDING_MAX_RETRIES=3

# ---- Qdrant ---------------------------------------------------------------
# Local:  http://localhost:6333
# Cloud:  https://xyz.cloud.qdrant.io
QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=            # only needed for Qdrant Cloud
QDRANT_COLLECTION=documents

# ---- Chunking -------------------------------------------------------------
CHUNK_STRATEGY=recursive
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# ---- Caching --------------------------------------------------------------
CACHE_EMBEDDING_ENABLED=true

# ---- Application ----------------------------------------------------------
APP_NAME=RAG Knowledge Assistant
APP_VERSION=1.0.0
APP_ENV=development
DEBUG=false

# ---- API Server -----------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api/v1
CORS_ORIGINS=*
